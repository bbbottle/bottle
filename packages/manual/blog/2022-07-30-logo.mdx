---
slug: logo
title: logo
authors: [bbki.ng]
---

import { OSS_PHOTOS } from "../constants/photos";
import { Img, Article } from '@bbki.ng/components';


## BBKi.ng Logo

<Img {...OSS_PHOTOS.logoPreview} renderedWidth={200} removeBlurBgAfterLoad />

### 制作步骤
1. 绘制草稿

<Img {...OSS_PHOTOS.logoSketch} removeBlurBgAfterLoad />

2. 绘制参考线

<Img {...OSS_PHOTOS.guidelines} removeBlurBgAfterLoad />

3. 填充颜色

<Img {...OSS_PHOTOS.logoAndGuidelines} removeBlurBgAfterLoad renderedWidth={300} />


### 目的

以下规范用于保证网站标识使用时，能得到最佳视觉效果。

### 规范

#### 颜色

<Img {...OSS_PHOTOS.logoColors} renderedWidth={600} removeBlurBgAfterLoad />

#### 尺寸
标识宽高不得小于 24 像素。

#### 禁区

禁区宽度为图标宽度的一半

<Img {...OSS_PHOTOS.logoExcludesZone} renderedWidth={300} />

#### 合法变体

##### 纯色背景

<Img {...OSS_PHOTOS.logoVariants} renderedWidth={600} removeBlurBgAfterLoad />

##### 图片背景
<Img {...OSS_PHOTOS.validLogoExample} renderedWidth={300} />

#### 非法误用

- 禁止旋转
- 禁止前景背景色出现合法变体外的组合
- 禁止拉伸、压缩
- 禁止使用描边形式
- 禁止修改增删
- 禁止图标越过禁区
- 禁止添加文字
- 禁止添加投影

#### 错误示例

<Img {...OSS_PHOTOS.badLogoExamples} renderedWidth={601} removeBlurBgAfterLoad />
